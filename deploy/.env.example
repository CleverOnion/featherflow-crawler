# FeatherFlow 环境变量配置文件
# 复制此文件为 .env 并根据需要修改配置

# =============================================================================
# 数据库配置（必须配置）
# =============================================================================
# MySQL数据库主机地址（如果是本地数据库可以使用localhost或127.0.0.1）
MYSQL_HOST=39.105.209.44
# MySQL端口（默认3306）
MYSQL_PORT=10001
# MySQL用户名
MYSQL_USER=root
# MySQL密码（必须提供）
MYSQL_PASSWORD=Ebashi123456@
# 数据库名称（如果不存在会自动创建）
MYSQL_DATABASE=tiangenexora

# =============================================================================
# 业务配置
# =============================================================================
# 要爬取的关键词，多个用逗号分隔
KEYWORDS=鹅,玉米,豆粕
# Cron表达式：分 时 日 月 周（5段格式），例如：30 8 * * * 表示每天8:30执行
CRON=0 * * * *
# 是否在启动时立即执行一次（1=是，0=否）
RUN_ON_START=1

# =============================================================================
# HTTP/爬虫配置
# =============================================================================
# HTTP请求超时时间（秒）
HTTP_TIMEOUT_SECONDS=20
# HTTP重试次数
HTTP_RETRY_TIMES=2
# 是否启用Playwright作为后备方案（1=是，0=否）
ENABLE_PLAYWRIGHT_FALLBACK=1
# Playwright是否使用无头模式（1=是，0=否）
PLAYWRIGHT_HEADLESS=1

# =============================================================================
# 反爬虫配置
# =============================================================================
# 退避算法基础时间（秒）
BACKOFF_BASE_SECONDS=10
# 退避算法最大时间（秒）
BACKOFF_MAX_SECONDS=600
# 检测到反爬时的最大重试次数
BLOCKED_MAX_RETRY=2

# =============================================================================
# 其他配置
# =============================================================================
# 日志级别（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO
# 时区
TZ=Asia/Shanghai